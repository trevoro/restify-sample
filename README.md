# Restify Sample

This is an example application that shows you how to do parameter validation
using Joi and the "throw an exception" style vs the continuation passing style.

It's much simpler to build an API this way.
